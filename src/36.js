// This is an example of how you can write a simple JavaScript function that adds two numbers.
function addNumbers(num1, num2) {
    return num1 + num2;
}

// Example usage:
const result = addNumbers(5, 3);
console.log(result); // Output: 8
